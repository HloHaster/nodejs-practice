<h1 style="margin-bottom: 10px; margin-top: 20px;">TAGS</h1>

{{#if haveTags}}
    <div class="posts" style="display: flex; flex-wrap: wrap; justify-content: space-between">
        {{#each tags}}
            <div class="card" style="width: 47%; margin-bottom: 10px;">
                <div class="card-body">
                    <h5 class="card-title" style="font-weight: bold;"><a href="tags/{{_id}}">{{name}}</a></h5>
                </div>
            </div>
        {{/each}}
    </div>
{{else}}
    <div>Nothing Found</div>
{{/if}}